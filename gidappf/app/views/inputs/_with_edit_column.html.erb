<table>
  <thead>
    <tr>
      <th>Id</th>
      <th><%= t('body.gidappf_entity.input.attributes.title')  %></th>
      <th><%= t('body.gidappf_entity.input.attributes.summary')  %></th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% inputs.each do |input| %>
      <tr>
        <td><%= input.documents.first.id %></td>
        <td><%= link_to "#{input.title}", input %></td>
        <td>
          <% if input.summary.length > 50 then %>
            <%= input.summary.first(50)+"…" %>
          <% else %>
            <%= input.summary %>
          <% end %>
        </td>
        <td><%= link_to t('body.common_entity.action.edit'), edit_input_path(input) %></td>
        <td><%= link_to t('body.common_entity.action.destroy'), disable_input_path(input), data: { confirm: t('body.common_entity.action.comfirm_msg') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
