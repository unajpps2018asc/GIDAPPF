<%= form_with(model: usercommissionrole, local: true) do |form| %>
  <% if usercommissionrole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usercommissionrole.errors.count, "error") %> prohibited this usercommissionrole from being saved:</h2>

      <ul>
      <% usercommissionrole.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.text_field :role_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, :disabled => true %>
  </div>

  <div class="field">
    <%= form.label :commission_id %>
    <%= form.text_field :commission_id, :disabled => true  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
