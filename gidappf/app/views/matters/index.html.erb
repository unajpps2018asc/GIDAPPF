<p id="notice"><%= notice %></p>

<h1><%= t('body.gidappf_entity.matter.models')  %></h1>

<table>
  <thead>
    <tr>
      <th><%= t('body.gidappf_entity.input.attributes.name')  %></th>
      <th><%= t('body.gidappf_entity.input.attributes.description')  %></th>
      <th><%= t('body.gidappf_entity.input.attributes.enable')  %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @matters.each do |matter| %>
      <tr>
        <td><%= matter.name %></td>
        <td><%= matter.description %></td>
        <td><%= matter.enable %></td>
        <td><%= link_to t('body.common_entity.action.show'), matter %></td>
        <td><%= link_to t('body.common_entity.action.edit'), edit_matter_path(matter) %></td>
        <td><%= link_to t('body.common_entity.action.destroy'), matter, method: :delete, data: { confirm: t('body.common_entity.action.comfirm_msg') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "#{t('body.common_entity.action.new_her')} #{t('body.gidappf_entity.matter.model')}", new_matter_path %>
