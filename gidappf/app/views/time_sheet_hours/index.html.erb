<p id="notice"><%= notice %></p>
<% if RoleAccess.get_role_access(current_user) > 39.0 then %>
  <!-- <h2><%= "#{t('body.common_entity.action.new_pl')} #{t('body.gidappf_entity.time_sheet_hour.models')}"  %></h2>
  <table class='gidappf-table'>
    <thead>
      <tr>
        <th><%= t('body.gidappf_entity.class_room_institute.models')  %></th>
        <th><%= t('body.gidappf_entity.commission.models')  %></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>
          <table>
            <thead>
              <tr>
                <th><%= t('body.gidappf_entity.class_room_institute.attributes.name')  %></th>
                <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_monday')).split(' ').last.capitalize %></th>
                <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_tuesday')).split(' ').last.capitalize  %></th>
                <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_wednesday')).split(' ').last.capitalize  %></th>
                <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_thursday')).split(' ').last.capitalize  %></th>
                <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_friday')).split(' ').last.capitalize  %></th>
                <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_saturday')).split(' ').last.capitalize  %></th>
                <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_sunday')).split(' ').last.capitalize  %></th>
                <th colspan="3"></th>
              </tr>
            </thead>
            <tbody>
              <% @class_room_institutes.each do |class_room_institute| %>
                <tr>
                  <td><%= button_to( class_room_institute.to_list, parametrize_class_room_institute_path(class_room_institute), :method => :get, :disabled => disable_cri(class_room_institute.id)) %></td>
                  <% 7.times do |i_day|%>
                    <td><%= class_room_institute.free_time(i_day) %></td>
                  <% end %>
                  <td><%= link_to t('body.common_entity.action.show'), class_room_institute %></td>
                  <td><%= link_to t('body.common_entity.action.edit'), edit_class_room_institute_path(class_room_institute) %></td>
                  <td><%= link_to t('body.common_entity.action.destroy'), class_room_institute, method: :delete, data: { confirm: t('body.common_entity.action.confirm_msg') } %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </td>
        <td>
          <table>
            <thead>
              <tr>
                <th><%= t('body.gidappf_entity.commission.attributes.name')  %></th>
                <th colspan="3"></th>
              </tr>
            </thead>
            <tbody>
              <% @time_sheets.each do |time_sheet| %>
                <tr>
                  <td><%= button_to( time_sheet.commission.name, parametrize_time_sheet_path(time_sheet), :method => :get, :disabled => disable_ts(time_sheet.id) ) %></td>
                  <td><%= link_to t('body.common_entity.action.show'), time_sheet %></td>
                  <td><%= link_to t('body.common_entity.action.edit'), edit_time_sheet_path(time_sheet) %></td>
                  <td><%= link_to t('body.common_entity.action.destroy'), time_sheet, method: :delete, data: { confirm: t('body.common_entity.action.confirm_msg') } %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </td>
      </tr>
    </tbody>
  </table>
  <h2><%= link_to "#{t('body.common_entity.action.new')} #{t('body.gidappf_entity.time_sheet_hour.model')}", time_sheet_hours_multiple_new_url %></h2> -->

  <%= render 'modal_commissions' %>

  <br>

<% end %>
  <h1><%= t('body.gidappf_entity.time_sheet_hour.models')  %></h1>

<table class='gidappf-table'>
  <thead>
    <tr>
      <th><%= t('body.gidappf_entity.commission.model')  %></th>
      <th> pid </th>
      <th><%= t('body.gidappf_entity.time_sheet_hour.attributes.hour_occupied')  %></th>
      <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_monday')).split(' ').last.first(3).capitalize  %></th>
      <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_tuesday')).split(' ').last.first(3).capitalize  %></th>
      <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_wednesday')).split(' ').last.first(3).capitalize  %></th>
      <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_thursday')).split(' ').last.first(3).capitalize  %></th>
      <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_friday')).split(' ').last.first(3).capitalize  %></th>
      <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_saturday')).split(' ').last.first(3).capitalize  %></th>
      <th><%= (t('body.gidappf_entity.class_room_institute.attributes.available_sunday')).split(' ').last.first(3).capitalize  %></th>
      <th colspan="2"></th>
    </tr>
  </thead>
  <tbody>
    <% @time_sheet_hours.each do |time_sheet_hour| %>
      <tr>
        <td><%= link_to "#{time_sheet_hour.time_sheet.commission.name}",time_sheet_hour %></td>
        <td><%= time_sheet_hour.vacancy.occupant %></td>
        <td><%= time_sheet_hour.occupied_hour_fmt %></td>
        <td><%= if time_sheet_hour.monday then '✓' end %></td>
        <td><%= if time_sheet_hour.tuesday then '✓' end %></td>
        <td><%= if time_sheet_hour.wednesday then '✓' end %></td>
        <td><%= if time_sheet_hour.thursday then '✓' end %></td>
        <td><%= if time_sheet_hour.friday then '✓' end %></td>
        <td><%= if time_sheet_hour.saturday then '✓' end %></td>
        <td><%= if time_sheet_hour.sunday then '✓' end %></td>
        <td><%= link_to t('body.common_entity.action.edit'), edit_time_sheet_hour_path(time_sheet_hour) %></td>
        <td><%= link_to t('body.common_entity.action.destroy'), time_sheet_hour, method: :delete, data: { confirm: t('body.common_entity.action.confirm_msg') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
